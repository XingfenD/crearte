apiVersion: apps/v1
kind: Deployment
metadata:
  name: webgame-collection
  namespace: webgame-collection
  annotations:
    keel.sh/policy: force
    keel.sh/match-tag: "true"
    keel.sh/trigger: poll
    keel.sh/pollSchedule: "@every 5m"
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app: webgame-collection
  template:
    metadata:
      labels:
        app: webgame-collection
    spec:
      containers:
        - name: web
          image: ghcr.io/XingfenD/webgame-collection:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 2
            periodSeconds: 10
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              cpu: 200m
              memory: 128Mi
